# 334 -- copycat COPYCAT_COMMIT/STAGE/REVERT audit entries carry no workId, so an imported record's History tab never shows where it came from -- emit a per-work COPYCAT_COMMIT alongside the batch summary (WORK_CLONE is the precedent) -- follow-up to 299

Opened 2026-07-10.
